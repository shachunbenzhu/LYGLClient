数据库中用户表新增记住密码字段

滑动Slidingmenu不可以用

？从后台返回实体，不需要一个一个字段put，前台实体实现序列化后就可以放进bundle中（MainActivity中initData方法）

后台字段可以加颜色

字体颜色重叠

登录成功后积分加

用户中的游记id不需要

先进入后输入IP地址，连接完成后再进入APP

后台地址：http://171b2160i0.51mypc.cn:33760/LYGLSystem/login.action

目的地标识下载状态，游记标识收藏状态

写游记时首先选目的地然后选图片后内容已经不再了

stratrgyactivity中不可以getNationData() ;
		getDestinationData(firstNationName) ;传递，因为在getNationData启动一个线程，当结束后firstNationName不再了，需要将
		其传递到主线程handler中，然后赋值

添加游记时对一些数据的初始化，添加日期时加上前导0:2017-01-01,记得添加景点次数

写游记时没有选择花销和同伴，直接从数据库输的数据

游客游记保存，但是用户不保存，游客的用户id一直是1，在游客写游记时可以提示登录

为GridView和ListView设置点击颜色

目的地不正确，map中没有值,看看省份有没有查出来,315行initExpandableListAdapterData() ;debug调可以有map，但是跳过就没有

从数据库中取出图像数据后需要先进行拆分得到图像名称.后缀名（Tools.formatString）


activity_destinationdetile中不可以滑动
destinationdescription中scrollview和gallery冲突

strategycatalog.xml中下载到本地最开始出不来，现在好了，在布局中必须最后设置中间的滑动，让他在最上方的下方，在最下方的上方，然后充满父控件


分享对话框的位置设置？

页面加载中

用户表中游记id可以去掉

目的地中加入同一目的地其他用户的游记

问题详情页

注册时上传图片

游记表中的目的地应该是是目的地表中的id

游记详情中的无图模式怎么传值？

写游记的同时增加景点的人气值

没有为作者写回复

怎样把Bitmap传到后台

子线程中不可以用主线程的控件，必须在主线程中使用

目的地详情的滑动

MainActivity中查询国家省份景点是通过向服务器三次请求数据，怎么只请求一次，将数据在服务器整理后返回，用JSON返回list或map？

json返回对象实体？

目的地中如果没有城市显示网络未连接，应该是显示空图片

从目的地进入快速了解可以，但是从攻略进去不可以

游记详情中图片列表出不来
